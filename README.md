# Easy CLI tool for deno

Same as the `tree` command.

## Usage

```
$ deno run --allow-read --allow-run ./src/main.ts
```

## Install

```
$ deno install --allow-read --allow-run --force --name treeview ./src/main.ts
```

### Using

```
$ treeview
```
